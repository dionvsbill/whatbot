import {db} from '@/lib/db';export default async function Overview(){const [shops,users,orders,payouts]=await Promise.all([db.shop.count(),db.user.count(),db.order.count(),db.payout.count()]);return <div><h1 className="text-2xl font-bold">Admin overview</h1><div className="mt-6 grid gap-4 md:grid-cols-4">{[['Shops',shops],['Users',users],['Orders',orders],['Payouts',payouts]].map(([a,b])=><div className="rounded-2xl border bg-white p-5" key={a as string}><p className="text-sm text-slate-500">{a}</p><p className="mt-2 text-3xl font-bold">{b}</p></div>)}</div></div>