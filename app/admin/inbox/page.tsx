import {db} from '@/lib/db';export default async function Inbox(){const messages=await db.whatsappMessage.findMany({take:100,orderBy:{createdAt:'desc'},include:{shop:true,conversation:true}});return <div><h1 className="text-2xl font-bold">Multi-vendor inbox</h1><div className="mt-6 overflow-hidden rounded-2xl border bg-white">{messages.map(m=><div className="border-b p-4" key={m.id}><div className="flex justify-between"><strong>{m.shop.name}</strong><span className="text-xs">{m.direction}</span></div><p className="mt-1">{m.body}</p><p className="text-xs text-slate-500">{m.conversation.customerPhone}</p></div>)}{messages.length===0&&<p className="p-10 text-center text-slate-500">No WhatsApp messages received.</p>}</div></div>