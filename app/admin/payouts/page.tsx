import {db} from '@/lib/db';import {money} from '@/lib/format';export default async function Payouts(){const payouts=await db.payout.findMany({take:200,orderBy:{periodEnd:'desc'},include:{shop:true}});return <div><h1 className="text-2xl font-bold">Payouts</h1><div className="mt-6 overflow-hidden rounded-2xl border bg-white"><table className="w-full text-left text-sm"><thead className="bg-slate-50"><tr><th className="p-4">Shop</th><th className="p-4">Amount</th><th className="p-4">Status</th></tr></thead><tbody>{payouts.map(p=><tr className="border-t" key={p.id}><td className="p-4">{p.shop.name}</td><td className="p-4">{money(Number(p.amount))}</td><td className="p-4">{p.status}</td></tr>)}</tbody></table></div></div>