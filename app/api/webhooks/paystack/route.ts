import {NextResponse} from 'next/server';import crypto from 'crypto';import {db} from '@/lib/db';export async function POST(req:Request){const raw=await req.text();const sig=req.headers.get('x-paystack-signature');const key=process.env.PAYSTACK_SECRET_KEY;if(!key||!sig)return NextResponse.json({error:'Webhook not configured'},{status:400});const expected=crypto.createHmac('sha512',key).update(raw).digest('hex');if(!crypto.timingSafeEqual(Buffer.from(sig),Buffer.from(expected)))return NextResponse.json({error:'Invalid signature'},{status:401});const event=JSON.parse(raw);if(event.event==='charge.success'&&event.data?.reference)await db.order.updateMany({where:{paystackReference:event.data.reference,status:'PENDING_PAYMENT'},data:{status:'PAID'}});return NextResponse.json({received:true})}