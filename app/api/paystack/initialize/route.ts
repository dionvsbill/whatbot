import {NextResponse} from 'next/server';export async function POST(req:Request){const key=process.env.PAYSTACK_SECRET_KEY;if(!key)return NextResponse.json({error:'PAYSTACK_SECRET_KEY is not configured'},{status:503});const {email,amount,reference,callback_url}=await req.json();if(!email||!amount||!reference)return NextResponse.json({error:'email, amount and reference are required'},{status:400});const r=await fetch('https://api.paystack.co/transaction/initialize',{method:'POST',headers:{Authorization:`Bearer ${key}`,'Content-Type':'application/json'},body:JSON.stringify({email,amount:Math.round(Number(amount)*100),reference,callback_url})});return NextResponse.json(await r.json(),{status:r.ok?200:502})}