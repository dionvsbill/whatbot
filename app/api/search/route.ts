import {NextResponse} from 'next/server';import {db} from '@/lib/db';export async function GET(req:Request){const q=new URL(req.url).searchParams.get('q')?.trim()||'';if(!q)return NextResponse.json({products:[],shops:[]});const [products,shops]=await Promise.all([db.product.findMany({where:{OR:[{title:{contains:q,mode:'insensitive'}},{sku:{contains:q,mode:'insensitive'}}]},select:{id:true,title:true,price:true,images:true},take:10}),db.shop.findMany({where:{OR:[{name:{contains:q,mode:'insensitive'}},{slug:{contains:q,mode:'insensitive'}}]},select:{id:true,name:true,slug:true,logoUrl:true},take:10})]);return NextResponse.json({products,shops})}