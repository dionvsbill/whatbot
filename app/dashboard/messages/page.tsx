import {db} from '@/lib/db';export default async function Messages(){const messages=await db.whatsappMessage.findMany({orderBy:{createdAt:'desc'},take:100,include:{shop:true,conversation:true}});return <div><h1 className="text-2xl font-bold">Messages</h1><div className="mt-6 overflow-hidden rounded-2xl border bg-white">{messages.map(m=><div key={m.id} className="border-b p-4"><div className="flex justify-between"><strong>{m.shop.name}</strong><span className="text-xs">{m.direction}</span></div><p className="mt-1">{m.body}</p><p className="text-xs text-slate-500">{m.conversation.customerPhone}</p></div>)}{messages.length===0&&<p className="p-10 text-center text-slate-500">No WhatsApp messages yet.</p>}</div></div>