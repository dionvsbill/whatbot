import {db} from '@/lib/db';export default async function Analytics(){const [products,orders,customers]=await Promise.all([db.product.count(),db.order.count(),db.user.count({where:{role:'CUSTOMER'}})]);return <div><h1 className="text-2xl font-bold">Analytics</h1><div className="mt-6 grid gap-4 md:grid-cols-3">{[['Products',products],['Orders',orders],['Customers',customers]].map(([a,b])=><div className="rounded-2xl border bg-white p-6" key={a as string}><p className="text-sm text-slate-500">{a}</p><p className="mt-2 text-3xl font-bold">{b}</p></div>)}</div></div>