export type ImportedProduct={title:string;price:number;compareAtPrice?:number;description:string;images:string[];rating?:number;affiliateLink:string;externalId?:string};export async function importJumia(url:string):Promise<ImportedProduct>{const r=await fetch(url,{headers:{'User-Agent':'Mozilla/5.0 WhatBot product importer'},cache:'no-store'});if(!r.ok)throw new Error(`Jumia source returned HTTP ${r.status}; the product cannot be imported until the source permits access.`);const html=await r.text();const title=html.match(/<title[^>]*>(.*?)<\/title>/i)?.[1]?.replace(/\s+/g,' ').trim();if(!title)throw new Error('Jumia product metadata was not available.');return{title,price:0,description:'Imported product requires price verification before publishing.',images:[],affiliateLink:url,externalId:url}}