import {createHmac,randomBytes,scryptSync,timingSafeEqual} from 'crypto';const secret=()=>process.env.AUTH_SECRET||'change-this-in-production';export function hashPassword(password:string){const salt=randomBytes(16).toString('hex');return `${salt}:${scryptSync(password,salt,64).toString('hex')}`}export function verifyPassword(password:string,stored:string){const [salt,key]=stored.split(':');if(!salt||!key)return false;const hash=scryptSync(password,salt,64);return timingSafeEqual(hash,Buffer.from(key,'hex'))}export function signSession(userId:string){const payload=Buffer.from(JSON.stringify({sub:userId,exp:Date.now()+2592000000})).toString('base64url');return `${payload}.${createHmac('sha256',secret()).update(payload).digest('base64url')}`}export function verifySession(token?:string){try{if(!token)return null;const [p,s]=token.split('.');const expected=createHmac('sha256',secret()).update(p).digest('base64url');if(!timingSafeEqual(Buffer.from(s),Buffer.from(expected)))return null;const d=JSON.parse(Buffer.from(p,'base64url').toString());return d.exp>Date.now()?d.sub:null}catch{return null}}