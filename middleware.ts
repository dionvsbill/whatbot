import {NextRequest,NextResponse} from 'next/server';import {verifySession} from './lib/security';export function middleware(req:NextRequest){const path=req.nextUrl.pathname;if(!path.startsWith('/dashboard')&&!path.startsWith('/admin'))return NextResponse.next();const id=verifySession(req.cookies.get('whatbot_session')?.value);if(!id)return NextResponse.redirect(new URL('/login',req.url));return NextResponse.next()}export const config={matcher:['/dashboard/:path*','/admin/:path*']};